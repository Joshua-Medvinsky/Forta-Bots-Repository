import { BigNumber, Wallet, providers } from "ethers";
export default class FortToken {
    private ethersPolygonProvider;
    private fortTokenAddress;
    constructor(ethersPolygonProvider: providers.JsonRpcProvider, fortTokenAddress: string);
    balanceOf(wallet: Wallet): Promise<BigNumber>;
    allowance(wallet: Wallet, spender: string): Promise<BigNumber>;
    approve(fromWallet: Wallet, spender: string, amount: BigNumber): Promise<any>;
    private getContract;
}
