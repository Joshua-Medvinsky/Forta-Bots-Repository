import { BigNumber, Wallet, providers } from "ethers";
export default class StakingContract {
    private ethersPolygonProvider;
    private stakingContractAddress;
    constructor(ethersPolygonProvider: providers.JsonRpcProvider, stakingContractAddress: string);
    activeStakeFor(agentId: string): Promise<any>;
    depositStake(fromWallet: Wallet, agentId: string, amount: BigNumber): Promise<any>;
    private getContract;
}
