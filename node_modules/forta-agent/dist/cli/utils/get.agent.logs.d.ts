import { AxiosStatic } from "axios";
export declare type GetAgentLogs = (agentId: string, minute?: Date) => Promise<FortaAgentLogResponse[]>;
export declare function provideGetAgentLogs(axios: AxiosStatic, fortaApiUrl: string): GetAgentLogs;
export interface FortaAgentLogResponse {
    scanner: string;
    timestamp: string;
    logs: string;
}
